/**
 * ScrollExport — a fire-and-forget plugin. The export runs in the button
 * listener (see index.js); this component exists only because
 * AppRegistry.registerComponent requires one. It is never displayed.
 *
 * @format
 */

import React from 'react';
import { View } from 'react-native';

function App(): React.JSX.Element {
  return <View />;
}

export default App;
